doctype html
html
  head
    meta(name="viewport" content="width=device-width, initial-scale=1" )
    title= title
    link(rel='stylesheet', href='/stylesheets/style.css')
  body
    header
      if user
        button.dropdown-btn
          <svg xmlns="http://www.w3.org/2000/svg"  width="56" height="47" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round"><path stroke="none" d="M0 0h24v24H0z" fill="none"/><path d="M4 6l16 0" /><path d="M4 12l16 0" /><path d="M4 18l16 0" /></svg>
        dialog
          button.closeDialog ❌
          ul
            li
              a(href="/posts/create") 🧾 Create New Post
            li
              a(href="/join_club") 👥 Join Club
      span Club House
    hr
    main
      block content
    script.
       const dialog = document.querySelector("dialog");
       const showButton = document.querySelector(".dropdown-btn");
       const closeButton = document.querySelector("dialog button");

       showButton.addEventListener("click", () => {
          console.log("opening dialog")
          dialog.showModal();
          });

       closeButton.addEventListener("click", () => {
          dialog.classList.add('slide-left');
          setTimeout(() => {
           dialog.classList.remove('slide-left');
           dialog.close();
            }, 500)
        });



